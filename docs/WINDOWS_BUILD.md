# Windows ?????

???? Windows ????? `launcher/`??? Electron + electron-builder ???Windows ??? NSIS ?????

## ????

- Windows x64
- Bun 1.4.0
- Node.js ?? `PATH` ????`launcher/scripts/package.cjs` ??? `node` ?? electron-builder?

??? Bun ???

```powershell
bun --version
```

?????

```text
1.4.0
```

## ????

???????????? launcher ??????

```powershell
cd C:\Users\Glimmer\codex-chatgpt-web-repo
bun install --frozen-lockfile
cd launcher
bun install --frozen-lockfile
cd ..
```

???? CI / Release ???????????????

## ?? Windows ???

?????????

```powershell
bun run --cwd launcher package:win
```

`package:win` ??????

1. TypeScript typecheck
2. Vite renderer production build
3. ???? runtime
4. electron-builder Windows ??
5. NSIS ??????

Windows ????? Windows ????`launcher/scripts/package.cjs` ????????????????????? Bun runtime?

## ????

????????????

```text
launcher\artifacts\
```

?????????? `launcher/package.json`?

```text
codex-web-gpt-<version>-win-x64.exe
```

?????? `5.0.9`?

```text
launcher\artifacts\codex-web-gpt-5.0.9-win-x64.exe
```

electron-builder ??????? `.blockmap` ??????????????? `.exe`?

?? NSIS ?????????????

```text
oneClick: false
perMachine: false
```

?????????????????????????????

## Smoke Test

?????????

```powershell
bun run --cwd launcher smoke:package
```

???????????

## ??????

??????????????

```powershell
bun run verify
```

Release CI ? Windows ?????????? AVX2-independent Bun runtime????? `verify`?`app:package` ? `app:smoke`???????? `.github/workflows/release.yml` ???????

## ??????

```powershell
cd C:\Users\Glimmer\codex-chatgpt-web-repo
bun install --frozen-lockfile
cd launcher
bun install --frozen-lockfile
cd ..
bun run --cwd launcher package:win
bun run --cwd launcher smoke:package
```

?????????? `package.json` ? `launcher/package.json` ?????????
